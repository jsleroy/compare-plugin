language: cpp

compiler:
  - gcc

env:
  - BUILDMODE=cmake-mingw64

before_script:
  - sudo add-apt-repository --yes ppa:ubuntu-toolchain-r/test
  - sudo add-apt-repository --yes ppa:tobydox/mingw-x-precise
  - sudo apt-get -qq update
  - sudo apt-get -qq install gcc-4.8 g++-4.8
  - sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-4.8 20
  - sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-4.8 20
  - sudo update-alternatives --config gcc
  - sudo update-alternatives --config g++
  - if [ "$BUILDMODE" = "cmake-mingw64" ]; then sudo apt-get -qq install mingw-w64; fi

script:
  - if [ "$BUILDMODE" = "cmake-mingw64" ]; then echo "===> Building with cmake+mingw64..." &&
      cd "$TRAVIS_BUILD_DIR" &&
      mkdir build &&
      cd build &&
      echo "SET(CMAKE_FIND_ROOT_PATH /opt/mingw64)" >> toolchain.cmake &&
      cmake -DCMAKE_TOOLCHAIN_FILE=toolchain.cmake .. &&
      make &&
      make install;
    fi